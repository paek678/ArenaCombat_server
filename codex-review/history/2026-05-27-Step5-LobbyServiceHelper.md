# Step 5: LobbyServiceHelper Extraction

## Date
2026-05-27

## Topic
Extract 11 duplicated try-catch blocks from LobbyManager into static LobbyServiceHelper class.

## Files Changed

| File | Action |
|------|--------|
| `Core/Network/LobbyServiceHelper.cs` | CREATE |
| `Core/Network/LobbyManager.cs` | MODIFY (710→644 lines) |

## Review Rounds

### Round 1: REVISE
1 critical issue:
- OnError event delegate passed directly as parameter snapshots at call time, not invocation time. Fix: added `FireError` instance method, pass method group instead.

1 non-issue flagged:
- PollLobbyAsync behavioral changes (LobbyConflict + general Exception catch) were pre-existing from disconnect fix, not introduced in Step 5.

### Round 2: PASS WITH NOTES
All issues resolved. Notes:
- FireError correctly reads OnError at invocation time via instance method group
- Lambda captures of instance fields through `this` are safe (immediate invocation)
- SendHeartbeatAsync local capture of lobbyId before lambda is correct
- QueryLobbiesAsync `?? new List<Lobby>()` preserves old return behavior
- .meta file auto-generated by Unity, no action needed
